<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- xpttsgoodsid[xpttsgoodsid]SQL自动映射 -->
<!--  注意:此文件由AOS平台自动生成-禁止手工修改 2017-12-11 09:55:53 -->
<mapper namespace="com.api.goods.dao.XpttsgoodsidDao">
	
	<!-- 插入一个数据持久化对象(插入字段为传入PO实体的非空属性) -->
	<insert id="insert"  parameterType="XpttsgoodsidPO">
		INSERT INTO xpttsgoodsid (
		<if test="goods_id != null and goods_id != ''">
	         goods_id, <!-- goods_id -->
		</if>
		<if test="id != null">
	          id  <!-- id -->
	   </if>
		)VALUES(
		<if test="goods_id != null and goods_id != ''">
	          #{goods_id, jdbcType=VARCHAR}, <!-- goods_id -->
	    </if>
		<if test="id != null">
		     #{id, jdbcType=INTEGER}  <!-- id -->
	    </if>
		)
	</insert>
	
	<!-- 插入一个数据持久化对象(含所有字段) -->
	<insert id="insertAll"  parameterType="XpttsgoodsidPO">
		INSERT INTO xpttsgoodsid (
		    <include refid="column" />
		)
		VALUES (
		    #{id, jdbcType=INTEGER},  <!-- id -->
		    #{goods_id, jdbcType=VARCHAR}  <!-- goods_id -->
		)
	</insert>

	<!-- 根据主键修改数据持久化对象 -->
	<update id="updateByKey" parameterType="XpttsgoodsidPO">
		UPDATE xpttsgoodsid
		<set>
			<if test="id != null">
		          id = #{id, jdbcType=INTEGER},  <!-- id -->
			</if>
			<if test="goods_id != null">
		          goods_id = #{goods_id, jdbcType=VARCHAR},  <!-- goods_id -->
			</if>
		</set>
		WHERE  id = #{id}
	</update>
	
	<!-- 根据主键删除数据持久化对象 -->
	<delete id="deleteByKey">
		DELETE FROM xpttsgoodsid WHERE  id = #{id}
	</delete>

	<!-- 根据主键查询并返回数据持久化对象 -->
	<select id="selectByKey" resultType="XpttsgoodsidPO">
		SELECT
		<include refid="column" />
		FROM xpttsgoodsid WHERE  id = #{id}
	</select>

	<!-- 根据唯一组合条件查询并返回数据持久化对象 -->
	<select id="selectOne" parameterType="Dto" resultType="XpttsgoodsidPO">
		SELECT
		    <include refid="column" />
		FROM xpttsgoodsid
		<where>
		    <include refid="equal" />
		</where>		
	</select>

	<!-- 根据Dto查询并返回数据持久化对象集合 -->
	<select id="list" parameterType="Dto" resultType="XpttsgoodsidPO">
		SELECT
			<include refid="column" />	
		FROM xpttsgoodsid
		<where>
		    <include refid="equal" />
		</where>	
	</select>

	<!-- 根据Dto查询并返回分页数据持久化对象集合 -->
	<select id="listPage" parameterType="Dto" resultType="XpttsgoodsidPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM xpttsgoodsid
		<where>
		    <include refid="equal" />
		</where>	
	</select>
	
	<!-- 根据Dto模糊查询并返回数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配) -->
	<select id="like" parameterType="Dto" resultType="XpttsgoodsidPO">
		SELECT
			<include refid="column" />	
		FROM xpttsgoodsid
		<where>
		    <include refid="like" />
		</where>	
	</select>

	<!-- 根据Dto模糊查询并返回分页数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配) -->
	<select id="likePage" parameterType="Dto" resultType="XpttsgoodsidPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM xpttsgoodsid
		<where>
		    <include refid="like" />
		</where>	
	</select>
	
	<!-- 根据Dto统计行数 -->
	<select id="rows" resultType="Integer" parameterType="Dto">
		SELECT COUNT(1) AS aos_rows_ FROM xpttsgoodsid
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 根据数学表达式进行数学运算 -->
	<select id="calc" parameterType="Dto" resultType="String">
		SELECT
			${_expr}
		FROM xpttsgoodsid
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 表字段 -->
	<sql id="column">
		  id,  <!-- id -->
		  goods_id  <!-- goods_id -->
	</sql>
	
	<!-- 表字段(用于外表的关联时引用) -->
	<sql id="column2">
		  xpttsgoodsid.id,  <!-- id -->
		  xpttsgoodsid.goods_id  <!-- goods_id -->
	</sql>

	<!-- Where精确匹配字段 -->
	<sql id="equal">
	   <if test="id != null ">
		      AND id = #{id}  <!-- id -->
		</if>
	    <if test="goods_id != null and goods_id != ''">
		      AND goods_id = #{goods_id}  <!-- goods_id -->
		</if>
	</sql>
	
	<!-- Where模糊匹配字段 -->
	<sql id="like">
	    <if test="goods_id != null and goods_id != ''">
		      AND goods_id like '${goods_id}%'  <!-- goods_id -->
		</if>
	</sql>
	
</mapper>